{
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "init:sqlite": "node scripts/init_sqlite_db.js",
    "scrape:last2m": "node scripts/scrape-last-2-months.js",
    "dev:electron": "node scripts/dev-electron.js",
    "dev:api": "ELECTRON_API_PORT=44373 SQLITE_DB_PATH=$(pwd)/dist/shekelsync.sqlite USE_SQLITE=true node electron/dev-api-server.js",
    "dev:electron:anonymized": "SQLITE_DB_PATH=$(pwd)/dist/shekelsync-anonymized.sqlite USE_SQLITE=true node scripts/dev-electron.js",
    "dev:electron:real": "SQLITE_DB_PATH=$(pwd)/dist/shekelsync.sqlite USE_SQLITE=true node scripts/dev-electron.js",
    "test": "npm --prefix app run test:coverage && npm --prefix renderer run test:unit && npm --prefix renderer run test:e2e",
    "test:security": "npm --prefix app run test",
    "test:security:coverage": "npm --prefix app run test:coverage",
    "test:security:watch": "npm --prefix app exec -- vitest watch",
    "audit:security": "npm audit --audit-level=moderate && npm run test:security:coverage",
    "icons:icns": "node scripts/generate-icns.js",
    "logs:bundle": "node scripts/collect-logs.js",
    "benchmark:check": "node scripts/benchmarks/assert-benchmarks.js",
    "smoke:license:ipc": "node scripts/license-ipc-smoke.js",
    "migrate:license:email-only": "node scripts/migrations/upgrade_license_email_only.js"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1",
    "electron-reload": "^2.0.0-alpha.1",
    "http-proxy-middleware": "^3.0.5"
  }
}
